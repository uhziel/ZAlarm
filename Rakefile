# You can get a list of available rake tasks by running $ rake -T
#
# rake db:create_migration NAME=name_of_migration
# => creates a new migration file in db/migrate/
#
# rake db:migrate
# => apply all migrations to the database that have no been applied.
#
# rake db:rollback
# => undo the previous migration

require './app'
require 'sinatra/activerecord/rake'
